{
  "tag": "v2026.06.19",
  "date": "June 2026",
  "releasedAt": "2026-06-13",
  "sections": [
    {
      "title": "Dashboard reliability",
      "items": [
        {
          "badge": "fix",
          "text": "<strong>Init stops on load failure</strong> — a failed bootstrap fetch shows a reload toast and an in-dashboard error panel with <strong>Reload</strong>; <code>window.dashboardInstance</code> is set so retry works."
        },
        {
          "badge": "fix",
          "text": "<strong>HTTP checks on startup</strong> — <code>/api/pages</code>, <code>/api/settings</code>, <code>/api/finders</code>, and <code>/api/bookmarks?all=true</code> verify <code>response.ok</code> before parsing JSON."
        },
        {
          "badge": "fix",
          "text": "<strong>Inline-edit guard</strong> — <code>renderDashboard()</code> and <code>loadPageBookmarks()</code> skip or confirm while unsaved inline edits are open; Quick Add and <code>:new</code> no longer desync the editor."
        },
        {
          "badge": "fix",
          "text": "<strong>Config sync retry that works</strong> — bookmark reloads use <code>{ rethrow: true }</code> inside <code>withRetry</code>; failed structure/settings refresh shows a retry toast instead of rendering stale data."
        },
        {
          "badge": "fix",
          "text": "<strong>Pending config sync</strong> — <code>reconcilePendingConfigSyncAfterLoad()</code> refreshes when a save landed after navigation; pending keys stay until sync succeeds."
        },
        {
          "badge": "fix",
          "text": "<strong>Config sync without reload</strong> — cross-tab structure/settings sync shows retry toasts instead of <code>window.location.reload()</code>; the <code>storage</code> listener ignores unrelated <code>localStorage</code> keys."
        },
        {
          "badge": "fix",
          "text": "<strong>Page rename &amp; bookmark move</strong> — page-tab rename and cross-page moves verify POST <code>response.ok</code> and roll back on failure."
        },
        {
          "badge": "fix",
          "text": "<strong>Corrupt device settings</strong> — bad <code>dashboardSettings</code> JSON in <code>localStorage</code> falls back to server settings instead of breaking init."
        }
      ]
    },
    {
      "title": "Dashboard performance & polish",
      "items": [
        {
          "badge": "new",
          "text": "<strong>Conditional all-bookmarks load</strong> — <code>/api/bookmarks?all=true</code> runs at startup only when global shortcuts, tag collections, or smart collections need cross-page data."
        },
        {
          "badge": "fix",
          "text": "<strong>Shared visual settings</strong> — dashboard loads <code>visual-settings.js</code>; background presets and opacity/animation helpers are no longer duplicated in <code>dashboard.js</code>."
        },
        {
          "badge": "fix",
          "text": "<strong>Deferred non-critical scripts</strong> — tours, onboarding, analytics, what's-new stub, and PWA hint use <code>defer</code> for faster first paint."
        },
        {
          "badge": "fix",
          "text": "<strong>Single locale fetch</strong> — removed the duplicate <code>loadTranslations</code> block from <code>dashboard.html</code>; <code>language.init()</code> handles i18n."
        },
        {
          "badge": "fix",
          "text": "<strong>Weather line safety</strong> — location/condition text uses <code>textContent</code> instead of <code>innerHTML</code>."
        },
        {
          "badge": "fix",
          "text": "<strong>Auto-dark listener guard</strong> — the dashboard fallback for <code>autoDarkMode</code> attaches one <code>matchMedia</code> listener instead of stacking on every settings apply."
        },
        {
          "badge": "fix",
          "text": "<strong>Tag cloud teardown</strong> — <code>DashboardTagCloud.destroy()</code> removes global keydown/resize listeners on <code>pagehide</code>."
        }
      ]
    },
    {
      "title": "Dashboard security",
      "items": [
        {
          "badge": "fix",
          "text": "<strong>Safe bookmark links</strong> — main grid and recent-modal links use <code>safeHttpResourceUrl</code>; <code>javascript:</code> and <code>data:</code> URLs cannot navigate."
        },
        {
          "badge": "fix",
          "text": "<strong>Empty-state escaping</strong> — page names, labels, hints, and category-empty copy use <code>escapeHtml</code> or <code>textContent</code> instead of raw <code>innerHTML</code>."
        },
        {
          "badge": "fix",
          "text": "<strong>Tip sanitization</strong> — rotating header tips allow only <code>&lt;code&gt;</code> and safe internal <code>&lt;a class=\"button-hint-link\"&gt;</code> links."
        }
      ]
    }
  ]
}
